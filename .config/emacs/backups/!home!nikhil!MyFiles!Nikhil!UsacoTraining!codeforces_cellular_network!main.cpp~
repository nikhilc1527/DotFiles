#include <algorithm>
#include <array>
#include <bitset>
#include <cmath>
#include <fstream>
#include <iostream>
#include <map>
#include <set>
#include <sstream>
#include <string>
#include <unordered_set>
#include <vector>

int main(int argc, char *argv[]) {
  int n,m;
  std::cin >> n >> m;
  std::vector<int> cities(n);
  for (int i = 0; i < n; ++i) {
    std::cin >> cities[i];
  }

  std::vector<int> towers(m);
  for (int i = 0; i < m; ++i) {
    std::cin >> towers[i];
  }

  int r = 0;
  int j = 0;
  for (int i = 0; i < n; ++i) {
    
  }

  
  return 0;
}
